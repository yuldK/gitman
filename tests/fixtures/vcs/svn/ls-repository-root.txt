# Apache Subversion 1.14 `svn ls` 기본 출력 계약을 근거로 작성했다.
# 출처: https://svnbook.red-bean.com/en/1.8/svn.ref.svn.c.list.html
# 디렉터리는 `/`로 끝나고 파일은 접미사가 없다. 실제 `svn.exe` 출력과 대조하지
# 않았다. `#`로 시작하는 줄은 test 도우미가 버린다.
branches/
README.txt
tags/
trunk/
공용 자료/
logo.png
